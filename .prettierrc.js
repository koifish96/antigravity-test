module.exports = {
    // 한 줄의 최대 길이를 100자로 제한 (기본값: 80)
    // 코드가 너무 길어지면 읽기 어려워지므로 적절한 길이로 줄바꿈을 유도합니다.
    printWidth: 100,

    // 탭 하나의 너비를 공백 2칸으로 설정 (기본값: 2)
    // 들여쓰기 간격을 조절하여 코드의 계층 구조를 시각적으로 표현합니다.
    tabWidth: 2,

    // 문자열을 사용할 때 작은따옴표(') 사용 (기본값: false)
    // 자바스크립트에서는 주로 작은따옴표를 선호하는 경향이 있습니다.
    singleQuote: true,

    // 여러 줄로 나열된 항목의 끝에 쉼표를 붙이지 않음 (기본값: "es5")
    // "none": 쉼표를 붙이지 않음, "es5": ES5에서 유효한 곳에만 붙임, "all": 가능한 모든 곳에 붙임
    trailingComma: 'none',

    // 객체 리터럴의 중괄호({}) 내부에 공백을 추가 (기본값: true)
    // 예: { foo: bar } (true) vs {foo: bar} (false)
    // 코드의 가독성을 위해 공백을 주는 것이 일반적입니다.
    bracketSpacing: true,

    // 문장의 끝에 세미콜론(;)을 추가 (기본값: true)
    // 자바스크립트의 자동 세미콜론 삽입(ASI) 기능에 의존하지 않고 명시적으로 문장을 종료합니다.
    semi: true,

    // 탭 문자 대신 스페이스를 사용하여 들여쓰기 (기본값: false)
    // 모든 환경에서 동일한 들여쓰기 간격을 보장하기 위해 스페이스 사용을 권장합니다.
    useTabs: false,

    // 화살표 함수의 매개변수가 하나일 때 괄호를 생략 (기본값: "always")
    // "avoid": x => x, "always": (x) => x
    // 코드를 좀 더 간결하게 표현할 수 있습니다.
    arrowParens: 'avoid',

    // 줄바꿈 문자를 LF(\n)로 통일 (기본값: "lf")
    // 운영체제(Windows, Mac, Linux) 간의 줄바꿈 문자 차이로 인한 Git 변경 사항 발생을 방지합니다.
    // "lf": Mac/Linux 스타일, "crlf": Windows 스타일
    endOfLine: 'lf'
};
